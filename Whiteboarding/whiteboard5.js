// given an array of 3 numbers returns whether or not the sum of those numbers is less than 20 return true
// otherwise return false
// output to be boolean

let arr1 = [1 , 2 , 3]
let arr2 = [10, 11, 12]

// create a function that takes an array of 3 numbers as an argument
const arrayMoreThan20 = (arr) {
// create variable to hold result
let result = 0
// add the values at each index one to the other and get result
arr.forEach((value) => {
  
})
// conditional checks if sum is less than 20

// return true or false based on the conditional
}
